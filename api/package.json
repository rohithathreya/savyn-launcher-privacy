{
  "name": "savyn-beta-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Savyn beta signup via Google Groups",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

